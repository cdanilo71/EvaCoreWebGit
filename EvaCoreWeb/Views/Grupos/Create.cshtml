@model EvaCoreWeb.Models.Grupo

@{
    ViewData["Title"] = "Create";
}

<h2>Create</h2>

<h4>Grupo</h4>
<hr />
<div class="row">
    <div class="col-md-4">
        <form asp-action="Create">
            <div asp-validation-summary="ModelOnly" class="text-danger"></div>
            <div class="form-group">
                <label asp-for="dsc_grupo" class="control-label"></label>
                <input asp-for="dsc_grupo" class="form-control" />
                <span asp-validation-for="dsc_grupo" class="text-danger"></span>
            </div>
            <div class="form-group">
                <label asp-for="isn_status_grupo" class="control-label"></label>
                <select asp-for="isn_status_grupo" class="form-control"></select>
                <span asp-validation-for="isn_status_grupo" class="text-danger"></span>
            </div>
            <div class="form-group">
                <input type="submit" value="Create" class="btn btn-default" />
            </div>
        </form>
    </div>
</div>

<div>
    <a asp-action="Index">Back to List</a>
</div>

@section Scripts {
    @{await Html.RenderPartialAsync("_ValidationScriptsPartial");}
}
